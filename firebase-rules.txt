{
  "rules": {
    "ranking": {
      ".read": true,
      ".indexOn": ["score"],
      "$node": {
        ".write": "newData.exists()",
        ".validate": "newData.hasChildren(['nome', 'score', 'acertos', 'total', 'data'])",
        "nome": {
          ".validate": "newData.isString() && newData.val().length >= 2 && newData.val().length <= 12 && newData.val().matches(/^[a-zA-Z0-9À-ÿ ]+$/)"
        },
        "score": {
          ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 1575"
        },
        "acertos": {
          ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 15 && newData.val() <= newData.parent().child('total').val()"
        },
        "total": {
          ".validate": "newData.isNumber() && newData.val() === 15"
        },
        "data": {
          ".validate": "newData.isNumber() && newData.val() <= now + 60000"
        },
        "$other": {
          ".validate": false
        }
      }
    }
  }
}
